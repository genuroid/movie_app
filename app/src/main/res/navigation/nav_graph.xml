<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loadingScreenFragment">

    <fragment
        android:id="@+id/loadingScreenFragment"
        android:name="com.example.final_app.LoadingScreenFragment"
        android:label=""
        tools:layout="@layout/fragment_loadingscreen">
        <action
            android:id="@+id/action_loadingScreenFragment_to_movieListFragment"
            app:destination="@id/movieListFragment"
            app:popUpTo="@id/loadingScreenFragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/movieListFragment"
        android:name="com.example.final_app.ui.list.MovieListFragment"
        android:label="Movie"
        tools:layout="@layout/fragment_movie_list">

        <action
            android:id="@+id/action_movieListFragment_to_movieDetailsFragment"
            app:destination="@id/movieDetailsFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out" />
    </fragment>

    <fragment
        android:id="@+id/movieDetailsFragment"
        android:name="com.example.final_app.ui.details.MovieDetailsFragment"
        android:label="Details"
        tools:layout="@layout/fragment_movie_details">

        <argument
            android:name="title"
            app:argType="string" />

        <argument
            android:name="overview"
            app:argType="string" />

        <argument
            android:name="posterPath"
            app:argType="string" />

        <argument
            android:name="rating"
            app:argType="float" />
    </fragment>

</navigation>